- expand games.db to fit columns from external games. make them nullable.

- fix ghastly styling on menus at some point, make it smaller and more verbose.

- migrate all js to typescript

- consider using an sqlite ORM like sequelize

